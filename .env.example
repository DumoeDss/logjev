# Secrets only — provider topology lives in config.yaml (api_key_env names variables here).
OPENROUTER_API_KEY=sk-or-...
# DEEPSEEK_API_KEY=sk-...
# NIM_API_KEY=nvapi-...
# TYPESAFE_API_KEY=tsk-...   # official Jev API (config.yaml "jev" passthrough provider)

# Auth for the bridge itself (optional; when set, POST /v1/systemone requires a Bearer token)
# BRIDGE_API_KEY=
